#!/bin/sh

# Common part
docker-perm-suff

PERM_OK=${?}

if [ ${PERM_OK} -ne 0 ]; then
    exit
fi

WGET=`which wget || echo /dev/null`

if [ -x ${WGET} ]; then
    echo "wget -qO- https://get.docker.com/ | sh"
fi
